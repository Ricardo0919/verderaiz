version: '3.8'
services:
  web:
    build:
      context: .
    ports:
      - "3000:3000"
    networks:
      - frontend
    environment:
      - NODE_ENV=production

networks:
  frontend:
    driver: bridge
